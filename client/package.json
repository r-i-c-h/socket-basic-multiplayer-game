{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "styles": "node-sass --watch --source-map true styles/index.scss styles/main.css --style=expanded",
    "dev": "concurrently \"node-sass --watch --source-map true styles/index.scss styles/main.css --style=expanded\" \"live-server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0",
    "eslint": "^7.2.0",
    "eslint-plugin-react": "^7.20.0"
  }
}
